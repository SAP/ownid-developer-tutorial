{
  "name": "ownid-developer-tutorial",
  "version": "1.0.0",
  "repository": "git@github.wdf.sap.corp:OwnID/docs.git",
  "author": "SAP",
  "license": "MIT",
  "keywords": [
    "OwnID"
  ],
  "scripts": {
    "start": "light-server -s tutorial --historyindex '/index.html' -p 3000 -w 'tutorial/** #echo reload the page'",
    "start:demo": "light-server -s demo --historyindex '/index.html' -p 3000 -w 'demo/** #echo reload the page'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "light-server": "^2.8.0"
  }
}
